function [sm_TM_CP_prevmat, sm_TM_CP_currmat, is_two] = sm_TM_main_comm (sm_output_k, sm_output_k-1)
% comm_id is the set of the common star ids between the two previous frames along with their star ids.
% is_two is a boolean, which is true if len(comm_id) >= 2, else false

is_two = True

end